import { Injectable } from '@nestjs/common';

/**
 * Handles refresh token issuance, rotation, and revocation.
 * Full implementation is part of the OAuth 2.0 token endpoint.
 */
@Injectable()
export class RefreshTokenService {}
